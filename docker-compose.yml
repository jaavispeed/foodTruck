services:
  db:
    image: postgres:14.3
    restart: always
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      TZ: 'America/Santiago'
      PGTZ: 'America/Santiago'
    ports:
      - '5432:5432'
    volumes:
      - ./postgres:/var/lib/postgresql/data
